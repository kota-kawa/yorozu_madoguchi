POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=yorozu
DATABASE_URL=postgresql://user:password@localhost:5432/yorozu
# Frontend API base (set when frontend and backend are on different origins)
# Example: https://api.example.com
VITE_API_BASE=
# Backend allowed origins (comma-separated)
# Example: https://chat.example.com,https://www.chat.example.com
ALLOWED_ORIGINS=
# Backend origin fallback
FRONTEND_ORIGIN=
# Add other keys from .env here if needed
